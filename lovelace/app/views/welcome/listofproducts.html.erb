<title>Lovelace | Produtos</title>

<%= render 'header' %>

<!-- start top_bg -->
<div class="top_bg">
<div class="wrap">
<div class="main_top">
	<h2 class="style">Produtos</h2>
</div>
</div>
</div>
<!-- start main -->
<div class="main_bg">
<div class="wrap">
<div class="main">
	
	<!-- start grids_of_3 -->
	<div class="grids_of_3">	
		<% @cont = 0%>
		<% @products.each do |product|%>
			<div class="grid1_of_3">				
				<% if @cont < 3 %>	 
				<% @cont = @cont+1 %> 			
				<a href="show.html">
					<%= link_to image_tag(product.image.url(:medium)), product %>
					<h3><%= product.name %></h3>
					<span class = "price"> R$<%= product.price %></span>
	  			<% end %>	
	  			</a>	  
			</div>
		<% end %>
	</div>
	<div class="clear"></div>

	<!-- start grids_of_3 -->
	<div class="grids_of_3">	
		<% @cont = 0%>
		<% @products.each do |product|%>
			<div class="grid1_of_3">				
				<% if @cont < 3 %>	 
				<% @cont = @cont+1 %> 			
				<a href="show.html">
					<%= link_to image_tag(product.image.url(:medium)), product %>
					<h3><%= product.name %></h3>
					<span class = "price"> R$<%= product.price %></span>
	  			<% end %>	
	  			</a>	  
			</div>
		<% end %>
	</div>
	<div class="clear"></div>
	
	
</div>
</div>
</div>
<%= render 'footer' %>
</body>
</html>